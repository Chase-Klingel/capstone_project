else if (/* looking for a vimeo user profile other than yourself (based on name they clicked) */)
  // axios.get('/api/videos/:username')
    // then some callback to set state for 'profileContent'
// last 'else if' could just be an 'else'
} else if (/* looking for sc user profile other than yourself (based on name they clicked) */) {
  // axios.get('/api/music/:username')
    // then some callback to set state for 'profileContent'
}
